import { StyleSheet, Text, View } from 'react-native';
import React from 'react';

type Props = {}

const DayScreen = (props: Props) => {
  return (
    <View>
      <Text>DayScreen</Text>
    </View>
  );
};

export default DayScreen;

const styles = StyleSheet.create({});